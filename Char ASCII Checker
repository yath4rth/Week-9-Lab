#include <stdio.h>

char is_digit(char c)
{
    if (c>='0' && c<='9')
    {
        return 1;
    }
    else
    {
        return 0;
    }
}

int main()
{

    char c;
    scanf(" %c", &c);
	printf("> %d", is_digit(c));
	
	return 0;

}
